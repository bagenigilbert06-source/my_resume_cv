# Gilbert Bageni — Résumé Portfolio

The React application is located in `gillz_resume_cv`.

## Development

```bash
cd gillz_resume_cv
pnpm install
pnpm start
```

Vercel deployment settings are defined in the root `vercel.json` file.
